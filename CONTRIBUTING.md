# 参与贡献

欢迎提交问题和改进，但请先保证修改可被验证。

## 开发流程

```bash
npm ci
npm run lint
npm test
npm run build:cloudflare
```

提交 Pull Request 时请说明：

- 解决了什么真实问题；
- 用户可见行为是否变化；
- 增加或修改了哪些测试；
- 是否影响额度、会员、登录、支付或 API 安全。

## 基本规则

- 不提交 API Key、密码、令牌、个人收款码和生产数据库标识。
- 不用本地模板冒充外部 AI 成功。
- 不补造转化率、销量、排名、亲测经历和保证性结论。
- 修改风格、长度、强度或会员权益时，必须让前端说明、服务端逻辑和测试保持一致。
- 修复内容应尽量小而明确，不顺带覆盖无关代码。
